import './Selector.css';

const Selector = () => {
  return (
    <div class="page-selector">
      <button>Search Recipes</button>
      <button>My Recipes</button>
    </div>
  )
}

export default Selector;