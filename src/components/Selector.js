import './Selector.css';

const Selector = () => {
  return (
    <div className="page-selector">
      <button>Recipes</button>
      <button>My Recipes</button>
    </div>
  )
}

export default Selector;